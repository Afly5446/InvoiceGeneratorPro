# Screenshots for README

Add **real** captures of the running app (recommended before sharing the repo publicly):

1. `main-window.png` — full window: form + live preview.
2. `form-validation.png` — optional: validation / error state.
3. `pdf-export.png` — optional: Save dialog or opened PDF sample.

Replace the placeholder images generated by `Resources/GenerateReadmeScreenshots.ps1` if you use that script, or drop PNG files here and keep the same filenames referenced from the root [README.md](../../README.md).

**Tip:** Windows Snipping Tool (`Win + Shift + S`) or `Alt + PrtScn` for the active window.
